[project]
name = "aitu-ai-n-nn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "md2pdf[cli]>=3.1.1",
]

[tool.uv.sources]
assignment1 = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]

